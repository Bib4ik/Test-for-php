services:
  php:
    build: .
    container_name: php-apache
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    networks:
      - app-network

networks:
  app-network:
    driver: bridge